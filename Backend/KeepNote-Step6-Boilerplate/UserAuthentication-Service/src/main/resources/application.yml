spring:
  datasource:
    #url: jdbc:mysql://${MYSQL_HOST}:3306/${MYSQL_DATABASE}
    url: jdbc:mysql://localhost:3306/keepnote6
    username: root
    password: root
    #username: ${MYSQL_USER}
    #password: ${MYSQL_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
server:
  port: 8089
